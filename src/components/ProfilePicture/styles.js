import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    borderRadius: 40,
    borderWidth: 3,
    borderColor: "#743cff",
    //margin: 5,
  },
  image: {
    borderRadius: 40,
    borderWidth: 3,
    borderColor: "white",
  },
});

export default styles;
