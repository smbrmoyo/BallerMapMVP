import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {},
  name: {
    textAlign: "center",
    marginVertical: 0,
    fontWeight: "bold",
    color: "white",
  },
});

export default styles;
